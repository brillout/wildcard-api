{
  "name": "@wildcard-api/client",
  "version": "0.3.1",
  "main": "index.js",
  "dependencies": {
    "@brillout/assert": "^0.1.1",
    "@brillout/fetch": "^0.1.2",
    "@brillout/json-s": "^0.1.2"
  },
  "description": "JavaScript Functions as API.",
  "repository": "https://github.com/reframejs/wildcard-api",
  "publishConfig": {
    "access": "public"
  },
  "scripts": {
    "prepublishOnly": "parcel build index.js --out-dir umd --out-file wildcard-api-client.production.min.js --global WildcardApiClient"
  },
  "browserslist": [
    "last 1 Chrome versions"
  ],
  "devDependencies": {
    "parcel": "^1.12.4"
  }
}
