{
  "private": true,
  "scripts": {
    "start": "npm run setup && npm run build && npm run server",
    "server": "node ./start-with-express.js",
    "build": "cd client && parcel build *.html",
    "setup": "npm run install-yarn && npm run install-deps",
    "install-yarn": "npm install --only=dev --no-save",
    "install-deps": "(cd .. && yarn && yarn link) && yarn link wildcard-api && yarn"
  },
  "dependencies": {
    "@hapi/inert": "^5.2.1",
    "babel-polyfill": "^6.26.0",
    "express": "^4.16.4",
    "fs-extra": "^7.0.1",
    "hapi": "^17.8.1",
    "knex": "^0.15.2",
    "koa": "^2.6.2",
    "koa-bodyparser": "^4.2.1",
    "koa-router": "^7.4.0",
    "koa-static": "^5.0.0",
    "parcel": "^1.12.3",
    "react": "16.7.0-alpha.2",
    "react-dom": "16.7.0-alpha.2",
    "sqlite3": "^4.0.3",
    "wildcard-api": "*"
  },
  "devDependencies": {
    "yarn": "1.12.3"
  }
}
